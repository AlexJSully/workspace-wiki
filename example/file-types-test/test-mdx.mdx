---
title: MDX Fixture
description: An MDX document with front matter
---

import { Note } from './Note';

# MDX Fixture

MDX combines Markdown with component syntax. This fixture exists so the wiki tree can be checked
against a file that is Markdown-shaped but carries the `.mdx` extension.

<Note>Components render only in an MDX toolchain; the wiki reads the front matter above.</Note>
