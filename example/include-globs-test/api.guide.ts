/**
 * API guide.
 *
 * This fixture exists so the wiki tree can be checked against a file included through a wildcard
 * pattern (`*.guide.ts`) rather than by its exact name.
 */
export const apiGuide = 'See the wiki tree for this file.';
