---
title: 'A Custom Title'
description: 'Shown as the tooltip when hovering this item in the tree'
---

# A Custom Title

Both `title` and `description` are set, so the tree shows the title and the hover shows the description.
